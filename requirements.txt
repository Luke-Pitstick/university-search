# Core dependencies
scrapy>=2.13.0
fire>=0.7.0
redis>=5.0.0
scrapy-redis>=0.7.0
beautifulsoup4>=4.12.0

# LangChain dependencies - using versions compatible with Python 3.14
# Note: Using older versions due to Python 3.14 compatibility issues with onnxruntime
langchain-core>=0.3.0,<1.0.0
langchain-chroma>=0.1.0,<1.0.0
langchain-openai>=0.1.0

# Vector database - using older version that doesn't require onnxruntime
chromadb>=0.4.0,<1.0.0

# Additional dependencies that may be needed
pydantic>=2.0.0,<3.0.0
pyyaml>=6.0.3